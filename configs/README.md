# Configs

This directory is for pipeline and experiment configuration.

It should store:

- pipeline step order;
- step parameters;
- profiles for quick and full runs;
- experiment-specific overrides.
