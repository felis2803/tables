{
  "run_id": "2026-03-31-baseline-validation",
  "command": "cargo run --release --",
  "input": "data/raw/tables.json",
  "output": "data/derived/tables.common_node_fixed_point.json",
  "report": "data/reports/report.common_node_fixed_point.json",
  "notes": [
    "Baseline validation run for the default fixed-point pipeline binary.",
    "Pipeline includes subset absorption, forced bits, pair reduction, and node filtering."
  ]
}
