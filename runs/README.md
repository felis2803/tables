# Runs

Each reproducible run should ideally get its own folder:

- `runs/<date>-<name>/config.json`
- `runs/<date>-<name>/summary.md`
- `runs/<date>-<name>/outputs/`

This keeps final named runs separate from ordinary derived artifacts.
